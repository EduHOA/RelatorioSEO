# Variáveis de ambiente (se necessário no futuro)

# API de tradução (para PDF em Inglês/Espanhol)
# URL do endpoint (formato LibreTranslate: POST /translate com q, source, target)
# VITE_TRANSLATE_API_URL=https://libretranslate.com/translate
# Chave opcional (alguns provedores exigem)
# VITE_TRANSLATE_API_KEY=
